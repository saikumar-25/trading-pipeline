# Secrets — NEVER commit these
local_settings.py
token_cache.json
.env

# local test artifact (cloud commits its own copy via git add -f)
oi_history.json

# Logs
*.log
pipeline.out.log
pipeline.err.log

# Python
__pycache__/
*.pyc
.venv/
venv/
