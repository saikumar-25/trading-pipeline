# Secrets — NEVER commit these
local_settings.py
token_cache.json
.env

# local test artifacts (cloud commits its own copies via git add -f)
oi_history.json
learning.json

# Logs
*.log
pipeline.out.log
pipeline.err.log

# Python
__pycache__/
*.pyc
.venv/
venv/
